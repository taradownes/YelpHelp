<% include ../partials/header  %>

<div class="container" style="max-width: 700px; margin: 0 auto;">
    <div class="row">
        <h1 class="col-md-12" style="padding: 40px">Edit <%= beach.name %></h1>
        <form class="col-md-12" action="/beaches/<%= beach._id %>?_method=PUT" method="POST">
            <div class="form-group">
              <input class="form-control" type="text" name="beach[name]" value="<%= beach.name %>" placeholder="name">
    beach
            <div class="fobeach
              <input class="form-control"type="text" name="beach[image]" value="<%= beach.image %>" placeholder="image url">
    beach
            <div class="forbeach
              <input class="form-control" type="text" name="beach[description]" value="<%= beach.description %>" placeholder="description">
            </div>
            <div class="form-group">
              <button class="btn btn-lg btn-block">Submit!</button>
            </div>
        </form>
    </div>


<a href="/beaches">Go Back</a>
</div>
<% include ../partials/footer  %>
